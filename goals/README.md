# Local Goal Queue

Put private local goals under `goals/queue/`. Goal files are ignored by Git so
you can write project-specific work without publishing it.

Use `goals/examples/example-goal.md` as the format reference.

