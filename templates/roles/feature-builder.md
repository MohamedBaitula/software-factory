# Feature Builder Role

Use this role for adding or improving product behavior.

- Start by reading the goal and existing project docs.
- Keep the implementation scoped to the requested feature.
- Add or update tests when behavior changes.
- Run validation before reporting completion.
- Stop if the product decision is unclear.

